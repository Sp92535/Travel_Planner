server:
  port: 6969

spring:
  datasource:
    url: jdbc:postgresql://localhost:5433/tripPlanner
    username: travel-admin
    password: roadroller
    driver-class-name: org.postgresql.Driver
  jpa:
    hibernate:
      ddl-auto: create-drop
    database: POSTGRESQL
    show-sql: true
    properties:
      hibernate:
        format-sql: true
    database-platform: org.hibernate.dialect.PostgreSQLDialect